// take s string c++
#include <bits/stdc++.h>

int main() {
    string s;
    cout << "Enter a string: ";
    cin >> s;
    cout << "You entered: " << s << endl;
    // Iterate over the string using a for loop
    for (int i = 0; i < s.length(); i++) {
        char ch = str[i];
        cout << ch << endl;
    }
    return 0;
}
